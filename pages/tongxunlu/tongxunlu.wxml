
<view>
  <view wx:for="{{arr}}" wx:key="aa" wx:for-item="info" class='hang'>
  <image mode="{{info.mode}}" src="{{info.image}}"></image>
  <view class='info' bindtap='click'>
    {{info.info}}
  </view>
  </view>
</view>
